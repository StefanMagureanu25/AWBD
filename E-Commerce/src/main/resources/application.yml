spring:
  profiles:
    active: development

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    org.hibernate.SQL: DEBUG
    org.springframework.security: DEBUG
    com.stefan.ecommerce.config.CustomUserDetailsService: DEBUG
    org.springframework.security.authentication: DEBUG